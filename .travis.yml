language: python

python:
    - 2.7

install:
    - sudo apt-get install llvm-2.9-dev clang
    - pip install nose
    - pip install coverage
    - pip install .

script:
    - python setup.py nosetests -s
